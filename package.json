{
    "name": "project-name",
    "version": "1.0.0",
    "main": "src/index.ts",
    "license": "MIT",
    "scripts": {
        "clean": "rimraf ./build",
        "build": "yarn clean && tsc",
        "start": "yarn build && node dist/index.js",
        "start:dev": "ts-node src",
        "start:watch": "nodemon"
    },
    "dependencies": {
        "@types/node": "^18.14.6",
        "typescript": "^4.9.5"
    },
    "devDependencies": {
        "nodemon": "^2.0.21",
        "rimraf": "^4.3.1",
        "ts-node": "^10.9.1"
    }
}
